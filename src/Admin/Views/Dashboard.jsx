import React from "react";

import { Layout } from './../Layout';

export const Dashboard = ({ children, ...props }) =>
{
    return (
        <Layout title="Home">
            content...
        </Layout>
    )
}
