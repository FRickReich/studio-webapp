import React from "react";

import { Layout } from './../../Layout';

export const Users = ({ children, ...props }) =>
{
    return (
        <Layout>
            Dashboard - Users
        </Layout>
    )
}
