import React from "react";

import { Layout } from './../Layout';

export const BlogEditor = ({ children, ...props }) =>
{
    return (
        <Layout backlink title="Neuer Blogeintrag">
            BlogEditor
        </Layout>
    )
}
