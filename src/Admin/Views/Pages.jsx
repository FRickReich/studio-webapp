import React from "react";

import { Layout } from './../../Layout';

export const Pages = ({ children, ...props }) =>
{
    return (
        <Layout title="Pages">
            content...
        </Layout>
    )
}
